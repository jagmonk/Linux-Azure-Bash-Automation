# GitHub Setup Guide

```bash
git init
git add .
git commit -m "Initial advanced Linux Azure Bash automation portfolio"
git branch -M main
git remote add origin <your-repo-url>
git push -u origin main
```

Before pushing, review all files for tenant IDs, subscription IDs, hostnames, usernames, private IPs, and secrets.
